name: Update Rules and Compile SRS

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.13'

      - name: Install sing-box
        run: |
          bash <(curl -fsSL https://sing-box.app/install.sh)

      - name: Run script
        run: python merge_and_compile.py

      # -----------------------------
      # Commit ALL outputs (JSON + SRS)
      # -----------------------------
      - name: Commit and push changes
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"

          git add \
            direct_rules.json direct_rules.srs direct_rules.list \
            proxy_rules.json proxy_rules.srs proxy_rules.list \
            reject_rules.json reject_rules.srs reject_rules.list \
            ip_rules.json ip_rules.srs ip_rules.list

          git commit -m "Auto-update: merged JSON + compiled SRS + LIST rules" || echo "No changes to commit"
          git push
